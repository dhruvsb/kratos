# Kratos — Session Handoff (2026-08-16)

**App:** Kratos - Voice Workout Logging · Apple ID **6801877279** · Bundle `com.dhruvshah.kratos` · Version **1.0** ("Prepare for Submission")
**Status:** All App Store Connect metadata is complete and saved. **No build has been uploaded yet — that is the blocker.** Nothing has been submitted for review.

---

## DONE this session (all verified live in App Store Connect)

1. **App Review Information — saved.** Sign-in required ON; demo account `appreview@kratos.app` / `KratosReview2026!`; contact Dhruv Shah, `+91 7874227770`, `dhruvsb@icloud.com`; full reviewer notes (how to sign in, core loop, voice→OpenAI flow). Fixed a phone-field validation error along the way.
2. **Pricing and Availability — saved.** Free ($0.00), available in all 175 countries/regions.
3. **EU Digital Services Act — completed.** Declared **non-trader**; ASC confirms "all regulatory requirements complete," active across 27 EU countries.
4. **Full re-verification of every ASC field** (via direct DOM reads, not just screenshots) → written up in `docs/app-store/APP-STORE-CONNECT-SUBMISSION-REPORT.md`. Confirmed already-saved: App Information (name/subtitle/categories/content-rights), version metadata (promo text/description/keywords/URLs/copyright), App Privacy labels (published, 6 data types, all linked, no tracking), Age Rating 4+, and the "not a regulated medical device" declaration.
5. **Screenshots — uploaded & saved.** 10 screenshots (1320×2868, 6.9" display) uploaded in correct narrative order: light Home → Settings → Routines → dark Home → Voice-consent → set-grid → keypad → logged set → Library → Progress. 6.5" slot auto-covered ("Using 6.9" Display"). Note: Apple caps a size at 10, so the 11th file (`11-exercise-progress2-dark.png`, a redundant 2nd progress chart) was left out.
6. **Corrected the build runbook** — `docs/app-store/BUILD-AND-UPLOAD-RUNBOOK.md`. Added the mandatory `expo prebuild --clean` step (the committed `ios/` folder is stale and would otherwise re-ship the compliance bugs the audit fixed) and corrected a wrong claim that screenshots were already uploaded.
7. **Resolved the "voice permission" confusion.** The live voice-logging feature (record.tsx → expo-audio → Supabase Edge Function → OpenAI) is fully intact; its mic permission is untouched. What the audit removed was a *separate, orphaned* `expo-speech-recognition` code path (`src/lib/stt.ts`, imported only by two files that nothing else renders) — a dead permission string, not the real feature.

---

## NOT DONE — remaining blockers (need a Mac + terminal + device; could not be done from this cloud session)

1. **Build & upload the app (HARD BLOCKER).** No build exists, so there is nothing to submit. Preferred path: `eas build -p ios --profile production && eas submit -p ios --latest` (EAS does a fresh prebuild, so the native compliance fixes ship automatically). Local-Xcode fallback is in the runbook, but requires `expo prebuild --clean` first.
2. **Confirm EAS env vars** `SUPABASE_URL` + `SUPABASE_ANON_KEY` are set for the `production` profile — else the build ships credential-less and nobody can sign in.
3. **Smoke-test the exact submitted build on a device:** demo account signs in on a clean install (and is seeded with sample workouts), full logging loop works, and **one real spoken voice log** works end-to-end (never tested on hardware; it's the headline feature the reviewer taps first).
4. **Keep the backend live through review:** `transcribe` + `parse-utterance` edge functions deployed and OpenAI key funded.
5. **Re-confirm both hosted URLs load** (privacy policy + support) the day of submission.
6. **Then: Add for Review** (Dhruv does this himself after final review).

---

## Why the build couldn't be done here (for context)
This session ran in the cloud. Its shell on the Mac is a Linux sandbox (no Xcode toolchain), and its screen-control of the Mac is restricted to click-only for Terminal/Xcode (can't type commands or keystrokes). iOS compile+sign+upload needs a macOS command line with keyboard control — so it must run either directly by a person, or via an **on-computer** Cowork session (desktop app → "Run this task" → On your computer).

## Reference files (all in `docs/app-store/`)
- `APP-STORE-CONNECT-SUBMISSION-REPORT.md` — every ASC field, re-verified.
- `BUILD-AND-UPLOAD-RUNBOOK.md` — corrected step-by-step build+upload.
- `PRE-SUBMISSION-AUDIT.md` — compliance audit + code fixes + manual checklist.
- `APP-STORE-LAUNCH-LOG.md`, `LISTING.md`, `COMPLIANCE-ANSWERS.md` — original plan & copy.
